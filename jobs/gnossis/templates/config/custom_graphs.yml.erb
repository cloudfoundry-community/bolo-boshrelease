<%
def mapify(v)
	return v unless v.is_a? OpenStruct
	return Hash[v.marshal_dump.map { |k,v| [k.to_s, mapify(v)] }]
end
%><%=
{
	"colors" => mapify(properties.bolo.gnossis.colors),
	"graphs" => mapify(properties.bolo.gnossis.graphs)
}.to_yaml
%>
